---
import Layout from '../../layouts/Layout.astro';
import { Header, Footer } from '@components/core';
import { ShopApp } from '@components/pages/Shop';
---

<Layout>
  <div class="w-full min-h-screen flex flex-col">
    <Header backbtn={true} />
    <div class="mt-35 grow">
      <ShopApp client:only/>
    </div>
    <Footer />
  </div>
</Layout>
